// Code generated by Fern. DO NOT EDIT.

package root_test

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	http "net/http"
	os "os"
	testing "testing"

	extend "github.com/extend-hq/extend-go-sdk"
	client "github.com/extend-hq/extend-go-sdk/client"
	option "github.com/extend-hq/extend-go-sdk/option"
	require "github.com/stretchr/testify/require"
)

func VerifyRequestCount(
	t *testing.T,
	testId string,
	method string,
	urlPath string,
	queryParams map[string]string,
	expected int,
) {
	wiremockURL := os.Getenv("WIREMOCK_URL")
	if wiremockURL == "" {
		wiremockURL = "http://localhost:8080"
	}
	WiremockAdminURL := wiremockURL + "/__admin"
	var reqBody bytes.Buffer
	reqBody.WriteString(`{"method":"`)
	reqBody.WriteString(method)
	reqBody.WriteString(`","urlPath":"`)
	reqBody.WriteString(urlPath)
	reqBody.WriteString(`","headers":{"X-Test-Id":{"equalTo":"`)
	reqBody.WriteString(testId)
	reqBody.WriteString(`"}}`)
	if len(queryParams) > 0 {
		reqBody.WriteString(`,"queryParameters":{`)
		first := true
		for key, value := range queryParams {
			if !first {
				reqBody.WriteString(",")
			}
			reqBody.WriteString(`"`)
			reqBody.WriteString(key)
			reqBody.WriteString(`":{"equalTo":"`)
			reqBody.WriteString(value)
			reqBody.WriteString(`"}`)
			first = false
		}
		reqBody.WriteString("}")
	}
	reqBody.WriteString("}")
	resp, err := http.Post(WiremockAdminURL+"/requests/find", "application/json", &reqBody)
	require.NoError(t, err)
	var result struct {
		Requests []interface{} `json:"requests"`
	}
	json.NewDecoder(resp.Body).Decode(&result)
	require.Equal(t, expected, len(result.Requests))
}

func TestParseWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &extend.ParseRequest{
		File: &extend.ParseRequestFile{
			FileFromURL: &extend.FileFromURL{
				URL: "https://example.com/bank_statement.pdf",
				Name: extend.String(
					"bank_statement.pdf",
				),
			},
		},
	}
	_, invocationErr := client.Parse(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestParseWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestParseWithWireMock", "POST", "/parse", nil, 1)
}

func TestEditWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &extend.EditRequest{
		File: &extend.EditRequestFile{
			FileFromURL: &extend.FileFromURL{
				URL: "https://example.com/form.pdf",
			},
		},
		Config: &extend.EditConfig{
			Instructions: extend.String(
				"Fill out the form with the provided data",
			),
			AdvancedOptions: &extend.EditConfigAdvancedOptions{
				FlattenPdf: extend.Bool(
					true,
				),
			},
		},
	}
	_, invocationErr := client.Edit(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestEditWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestEditWithWireMock", "POST", "/edit", nil, 1)
}

func TestExtractWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &extend.ExtractRequest{
		Config: &extend.ExtractConfigJSON{
			Schema: map[string]any{
				"properties": map[string]any{
					"invoice_number": map[string]any{
						"description": "The invoice number",
						"type":        "string",
					},
					"total_amount": map[string]any{
						"description": "The total amount due",
						"type":        "number",
					},
					"vendor_name": map[string]any{
						"description": "The name of the vendor",
						"type":        "string",
					},
				},
				"type": "object",
			},
		},
		File: &extend.ExtractRequestFile{
			FileFromURL: &extend.FileFromURL{
				URL: "https://example.com/invoice.pdf",
			},
		},
	}
	_, invocationErr := client.Extract(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestExtractWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestExtractWithWireMock", "POST", "/extract", nil, 1)
}

func TestClassifyWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &extend.ClassifyRequest{
		Config: &extend.ClassifyConfig{
			Classifications: []*extend.Classification{
				&extend.Classification{
					ID:          "invoice",
					Type:        "invoice",
					Description: "An invoice or bill for goods or services",
				},
				&extend.Classification{
					ID:          "receipt",
					Type:        "receipt",
					Description: "A receipt confirming payment",
				},
				&extend.Classification{
					ID:          "other",
					Type:        "other",
					Description: "Any other document type",
				},
			},
		},
		File: &extend.ClassifyRequestFile{
			FileFromURL: &extend.FileFromURL{
				URL: "https://example.com/document.pdf",
			},
		},
	}
	_, invocationErr := client.Classify(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestClassifyWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestClassifyWithWireMock", "POST", "/classify", nil, 1)
}

func TestSplitWithWireMock(
	t *testing.T,
) {
	WireMockBaseURL := os.Getenv("WIREMOCK_URL")
	if WireMockBaseURL == "" {
		WireMockBaseURL = "http://localhost:8080"
	}
	client := client.NewClient(
		option.WithBaseURL(WireMockBaseURL),
		option.WithToken("test-token"),
	)
	request := &extend.SplitRequest{
		Config: &extend.SplitConfig{
			SplitClassifications: []*extend.SplitClassification{
				&extend.SplitClassification{
					ID:          "invoice",
					Type:        "invoice",
					Description: "An invoice or bill for goods or services",
					IdentifierKey: extend.String(
						"invoice number from the document header",
					),
				},
				&extend.SplitClassification{
					ID:          "receipt",
					Type:        "receipt",
					Description: "A receipt confirming payment",
					IdentifierKey: extend.String(
						"receipt number",
					),
				},
				&extend.SplitClassification{
					ID:          "other",
					Type:        "other",
					Description: "Any other document type",
				},
			},
		},
		File: &extend.SplitRequestFile{
			FileFromURL: &extend.FileFromURL{
				URL: "https://example.com/multi-document.pdf",
			},
		},
	}
	_, invocationErr := client.Split(
		context.TODO(),
		request,
		option.WithHTTPHeader(
			http.Header{"X-Test-Id": []string{"TestSplitWithWireMock"}},
		),
	)

	require.NoError(t, invocationErr, "Client method call should succeed")
	VerifyRequestCount(t, "TestSplitWithWireMock", "POST", "/split", nil, 1)
}
